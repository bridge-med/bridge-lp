# BRIDGE — 開発ガイド

BRIDGEは理学療法士・橋本渉が個人で運営するプロジェクト。
「人が、自ら選び、納得して生きられる社会をつくる」ための思想と、その道具群のサイトである。

## 一次文書（すべての判断はここに照らす）

- `CONSTITUTION.md` — 憲法。思想・声・デザイン・UXの全基準。公開前の照合は義務（第34条）
- `docs/context.md` — プロジェクトコンテキスト。BRIDGEの対象者（対人援助職全般）・プロダクト思想・UX/文章方針・開発者の判断基準と仕事の進め方。**何かを作る・提案する前に必ずここに照らす**
- `docs/ia.md` — サイトの情報設計（IA v2.0）。ページ構成・ナビ・拡張ルール

憲法とcontext.mdで規定が異なる箇所（文体など）の棲み分けは暫定：このサイトの公開ページは憲法優先、プロダクトの設計・UI文言・提案の進め方はcontext.md優先。正式な整理は憲法改定（第33条）の検討課題。

ゲーム類型(clinic-flow-3d・turnaround12)の案内文はcontext.md §7(絵文字)・§8(ですます)の適用外。ただし憲法第8条・第15条・第19条3〜4号と三分法(制度上の事実/まだ判定できない/ゲーム上の仮定)は常に適用し、**文字量は抑える**(棲み分け=2026-09-04 社長発効、文字量=同日の社長指示。全文はdocs/context.md冒頭)。**名称は正式名称ベース**(2026-09-05 社長指示。減らすのは文の数と修飾であって制度上の名称ではない)。

## 体制（サブエージェント）

社長＝橋本渉。最終判断は常に社長。実装の実作業はメインのClaude（現場）が担う。

| エージェント | 役職 | 既定モデル | 使いどき |
|---|---|---|---|
| `pm` | 右腕・責任者 | inherit | 着手前の計画・優先順位・スコープ、出荷判定の取りまとめ |
| `editor` | 編集長 | opus | 文章を書く・直す・公開する前（第17〜20条） |
| `designer` | アートディレクター | opus | 見た目・UXの設計方針と照合（第21〜29条） |
| `qa` | 品質管理 | sonnet | コミット・公開直前の技術検証 |
| `auditor` | 監査役 | inherit | 定期総点検・方針転換前・憲法改定の起草 |
| `biz` | 事業開発 | inherit | 収益化・売上導線・ユーザーの声の経路の判断が要るとき |
| `pr` | 広報 | inherit | 発信（note等）の計画・告知・発信の振り返り |

流れの目安：着手前に `pm` → 制作（現場＋`editor`/`designer`/`biz`/`pr`に相談）→ 出荷前に `qa`＋領分の照合 → `pm` が総合判定 → 社長が公開を決める。

増員のルール：同種の判断業務が3回以上実際に発生し、既存の領分に収まらないときに、新しい役職を起案する（第14条・空室の禁止の組織版）。組織図を先に大きくしない。実務が先、役職が後。

モデルの采配はPMの職務（pm.md 6項）。既定より軽く済むタスクはPMの進言で下げ、出荷物の品質に関わる判断は下げない。呼び出し時の個別上書きが既定値に優先する。

## 技術メモ

- 静的サイト。ビルドなし。`shared/bridge.css` / `shared/bridge.js` が共通基盤
- ナビ・フッターは `shared/bridge.js` の NAV 配列が唯一の情報源。ページ追加はここに1行
- プロダクト一覧は `products/index.html` の PRODUCTS 配列、活動は `projects/index.html` の ACTIVITIES 配列
- ライト/ダークはトークン（`--bg` 等）差し替えのみで対応。色のハードコード禁止
- 文体・見出し・色の禁則は憲法第19条・第22条を参照。見出しに句点を付けない。「!」を使わない
- 実装・修正・改善の進め方は `dev-loop` スキル（`.claude/skills/dev-loop/SKILL.md`）に従う。依頼種別（feature/bugfix/ui-improvement/refactor/product-idea）ごとの調整もここに定める
- 出荷前の機械検査は `node scripts/check-site.mjs`。コミット時は `.claude/settings.json` のhookが自動で強制する。直せない既知の違反は `scripts/check-site.allowlist.json` に理由付きで隔離
- 出荷（コミット・公開）の手順は `ship` スキル（`.claude/skills/ship/SKILL.md`）に従う。判定の記録は `cockpit-76a805/shiplog.jsonl`

## セッション運用（PR監視の禁止）

- PRを作成しても、定期チェックの予約（send_later・Routine等でセッションを後から起こし直す仕組み）を自分で仕掛けない。イベント購読（subscribe_pr_activity）も同様
- PRの監視・自動フォローは、社長がその都度明示的に指示したときだけ行う
- 監視をやめる指示を受けたら、返事だけで終わらせない。サーバー側に登録済みの予約（Routine）を削除し、一覧で残存ゼロを確認してから報告する（2026-08-29の社長指示による恒久ルール）
