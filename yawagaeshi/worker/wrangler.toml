name = "yawagaeshi-api"
main = "worker.js"
compatibility_date = "2024-11-01"

# 使うモデル（安さ重視なら "gemini-2.5-flash-lite" でも可）
[vars]
GEMINI_MODEL = "gemini-2.5-flash"

# APIキーは vars ではなく Secret で設定する：
#   wrangler secret put GEMINI_API_KEY
